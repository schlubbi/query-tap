{
  "name": "QueryTap Dev",
  "image": "mcr.microsoft.com/devcontainers/go:1.23-bookworm",
  "runArgs": ["--privileged"],
  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {}
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y clang llvm libbpf-dev linux-headers-generic make && go mod download",
  "customizations": {
    "vscode": {
      "extensions": [
        "golang.go"
      ]
    }
  }
}
